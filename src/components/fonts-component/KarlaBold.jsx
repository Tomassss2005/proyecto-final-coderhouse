import { Text } from "react-native";

const KarlaBold = ({ children }) => {

    return (
        <Text style={{ fontFamily: "Roboto" }}>
            {children}
        </Text>
    )
}

export default KarlaBold;